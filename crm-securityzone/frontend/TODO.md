# TODO - FE Post Feed delete button

## Done
- [ ]

## To do (implement)
- [ ] Find post feed list rendering component (done conceptually: UnifiedDashboardHomeContent + PostCard).
- [x] Update `PostCard` to support a “Xóa” button and `onDelete` prop.
- [x] Add optimistic delete + rollback on API failure in UnifiedDashboardHomeContent.


- [ ] Update `UnifiedDashboardHomeContent` to implement optimistic delete + rollback on failure.
- [ ] Confirm dialog trước khi xóa.
- [ ] Toast lỗi khi delete fail (sonner).
- [ ] Run lint/build.

